export { traverse } from './traverse'
// const config = require('../fd.config.js') // 这个文件读不到
// const path = require('path')

// const { rootPath, output, template, extractOnly, whiteList } = config

// traverse({
//   outputPath: path.resolve(output.path, output.filename),
//   rootPath: rootPath,
//   template,
//   extractOnly,
//   whiteList
// })
